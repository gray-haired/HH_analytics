{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build"
    }
  ],
  "buildCommand": "pip install -r requirements.txt && mkdocs build --site-dir site",
  "outputDirectory": "site",
  "installCommand": "python -m pip install --upgrade pip"
}